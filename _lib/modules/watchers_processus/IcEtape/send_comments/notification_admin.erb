<%
  form = Form.new(id:'send-comments-form', route:route.to_s, libelle_size:140, watcher:id)
  rows = {}
  icetape.documents.each_with_index do |document, idx|
    rows.merge!( "#{document.original_name}" => {name: "document-#{document.id}-comments", type:'file'})
  end
  form.rows = rows
  form.submit_button = "Envoyer les commentaires"
%>
<div><%= icetape.ref.titleize %></div>
<%= form.out %>
